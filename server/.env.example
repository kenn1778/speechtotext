# Optional: Override the API Gateway URL (defaults to deployed dev endpoint)
# API_GATEWAY_URL=https://mytne8lapa.execute-api.us-east-1.amazonaws.com/dev/transcribe

# Server port (optional, defaults to 5174)
# PORT=5174
